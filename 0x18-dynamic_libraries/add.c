#include "main.h"
/**
 * add - adds two integers
 * @a: first integer
 * @b: second integer
 *
 * Description: adds two integers and returns the result
 * Return: int
 */
int add(int a, int b)
{
	int s;

	s = a + b;

	return (s);
}

