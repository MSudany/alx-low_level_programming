#include "main.h"
/**
 * add - multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Description: multiplies two integers and returns the result
 * Return: int
 */
int mul(int a, int b)
{
	int s;

	s = a * b;

	return (s);
}
